import { CardModel } from "./application-types";

const DEFAULT_CARDS: CardModel[] = [
  {
    id: 0,
    title: "Set up graham cracker and chocolate",
    created: new Date(2022, 6, 1),
    content: "It's nice to set up ahead of time",
  },
];

export { DEFAULT_CARDS };
